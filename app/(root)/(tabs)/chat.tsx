import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView className="text-center flex-1 justify-center items-center">
      <Text className="text-center">Chat</Text>
    </SafeAreaView>
  );
};

export default Chat;
